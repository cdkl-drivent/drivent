import styled from 'styled-components';

const SCSubContainer = styled.div`
  display: flex;
  flex-direction: column;
  gap: 17px;

  p {
    font-family: 'arial';
    font-size: 20px;
    line-height: 23px;
    color: #8e8e8e;
  }
`;

export { SCSubContainer };
