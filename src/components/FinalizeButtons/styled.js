import styled from 'styled-components';

const SCFinalizeButtons = styled.div`
  display: flex;
  gap: 30px;

  button {
    padding: 11px 13px;
    background-color: #e0e0e0;
    font-size: 14px;
    box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.25);
    border-radius: 4px;
    border: none;
  }
`;

export { SCFinalizeButtons };
